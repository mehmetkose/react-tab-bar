# ReactTabBar

## Install

    npm install react-tab-bar

## Usage

    import { ReactTabBar } from 'ReactTabBar';

    const ParentComponent = () => (
      <div>
        <ReactTabBar />
      </div>
    );    
